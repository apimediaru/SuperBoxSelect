/**
 * Grid Renderer for SuperSoxSelect TV
 *
 * @package superboxselect
 * @subpackage grid renderer
 * @return {string}
 */

SuperBoxSelect.Renderer = function (value) {
    if (!value.length) {
        return '';
    }

    //@todo

    return '';
};
